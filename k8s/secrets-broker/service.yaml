apiVersion: v1
kind: Service
metadata:
  name: secrets-broker
  namespace: cse474-prod
spec:
  type: ClusterIP
  selector:
    app: secrets-broker
  ports:
    - name: http
      port: 3001
      targetPort: 3001
