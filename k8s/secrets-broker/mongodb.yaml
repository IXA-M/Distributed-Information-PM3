apiVersion: apps/v1
kind: Deployment
metadata:
  name: secrets-broker-db
  namespace: cse474-prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: secrets-broker-db
  template:
    metadata:
      labels:
        app: secrets-broker-db
    spec:
      containers:
        - name: mongo
          image: mongo:7
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_DATABASE
              value: secrets_broker_db
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: secrets-broker-db
  namespace: cse474-prod
spec:
  selector:
    app: secrets-broker-db
  ports:
    - name: mongo
      port: 27017
      targetPort: 27017
