apiVersion: v1
kind: Service
metadata:
  name: chunk-catalog
  namespace: cse474-prod
spec:
  type: ClusterIP
  selector:
    app: chunk-catalog
  ports:
    - name: http
      port: 3001
      targetPort: 3001
