apiVersion: v1
kind: Service
metadata:
  name: chaos-simulator
  namespace: cse474-prod
spec:
  type: ClusterIP
  selector:
    app: chaos-simulator
  ports:
    - name: http
      port: 3002
      targetPort: 3002
