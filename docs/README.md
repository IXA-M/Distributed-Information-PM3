# Docs

Place shared documentation here.

API documentation:

- Chunk Catalog OpenAPI: `../services/chunk-catalog/openapi.yaml`
- Chunk Location OpenAPI: `../services/chunk-location/openapi.yaml`

Swagger UI endpoints:

- Chunk Catalog: `GET /api-docs` or `GET /docs`
- Chunk Location: `GET /api-docs` or `GET /docs`

Run the services locally or in Kubernetes, then open the matching service URL with `/api-docs`.
