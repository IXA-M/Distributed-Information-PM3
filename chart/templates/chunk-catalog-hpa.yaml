{{- if .Values.services.chunkCatalog.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.services.chunkCatalog.name }}
  namespace: {{ include "cse474.namespace" . }}
  labels:
    app.kubernetes.io/name: {{ .Values.services.chunkCatalog.name }}
    {{- include "cse474.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.services.chunkCatalog.name }}
  minReplicas: {{ .Values.services.chunkCatalog.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.services.chunkCatalog.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.chunkCatalog.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.chunkCatalog.autoscaling.targetMemoryUtilizationPercentage }}
{{- end }}
