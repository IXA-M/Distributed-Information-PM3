apiVersion: v1
kind: ConfigMap
metadata:
  name: distributed-information-config
  namespace: {{ include "cse474.namespace" . }}
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
